# =============================================================================
# FairSquare — Python dependencies
# Python >= 3.11  |  Install: pip install -r requirements.txt
# =============================================================================

# ── Core data stack ───────────────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26
scipy>=1.12              # required by SHAP TreeExplainer
pyarrow>=14.0            # parquet I/O (DVF processed datasets)
requests>=2.31
httpx>=0.27              # async HTTP (DVF download, OSM Overpass API)

# ── Geospatial ────────────────────────────────────────────────────────────────
# Requires GDAL system library:
#   Linux / Render : apt-get install libgdal-dev gdal-bin
#   macOS          : brew install gdal
#   Windows        : use conda (conda install -c conda-forge geopandas) or
#                    pre-built wheels from https://www.lfd.uci.edu/~gohlke/pythonlibs/
geopandas>=0.14
shapely>=2.0

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn>=1.5
lightgbm>=4.3
pygam>=0.9               # Generalised Additive Models (tournament baseline)
shap>=0.45               # SHAP TreeExplainer for feature attribution
joblib>=1.4              # model serialisation / parallel processing

# ── Hyperparameter Optimisation ───────────────────────────────────────────────
optuna>=3.6              # Bayesian HPO — TPE sampler, 40 trials (train_v5)

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0
asyncpg>=0.29            # async PostgreSQL driver
alembic>=1.13            # schema migrations

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30

# ── Frontend (Streamlit demo) ─────────────────────────────────────────────────
streamlit>=1.35
plotly>=5.22
folium>=0.16
streamlit-folium>=0.20
matplotlib>=3.7

# ── Vision / LLM ─────────────────────────────────────────────────────────────
google-genai>=1.0        # Google Gemini Vision API (renovation scorer)
Pillow>=10.3

# ── Web Scraping ─────────────────────────────────────────────────────────────
beautifulsoup4>=4.12
lxml>=5.0
cloudscraper>=1.2.71
firecrawl-py>=1.5        # AI-powered listing scraper (SeLoger, PAP, etc.)

# ── Report generation ─────────────────────────────────────────────────────────
markdown>=3.6            # Markdown -> HTML conversion
xhtml2pdf>=0.2.11        # Pure-Python HTML -> PDF (no GTK/Pango/Cairo needed)

# =============================================================================
# Dev / test extras
#   Option A (recommended): pip install -e ".[dev]"  (see pyproject.toml)
#   Option B:               pip install pytest pytest-asyncio pytest-cov ruff mypy
# =============================================================================

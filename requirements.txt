# ── Data ─────────────────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26
scipy>=1.12            # required by SHAP tree explainer
geopandas>=0.14
shapely>=2.0
pyarrow>=14.0          # parquet I/O
requests>=2.31
httpx>=0.27            # async HTTP (DVF download, OSM)

# ── Database ──────────────────────────────────────────────────────────
sqlalchemy>=2.0
asyncpg>=0.29          # async PostgreSQL driver
alembic>=1.13          # schema migrations

# ── Configuration ────────────────────────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ── Machine Learning ─────────────────────────────────────────────────
scikit-learn>=1.5
lightgbm>=4.3
pygam>=0.9
shap>=0.45
joblib>=1.4

# ── Vision / LLM ─────────────────────────────────────────────────────
google-genai>=1.0      # Google Gemini Vision API
Pillow>=10.3

# ── API ───────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30

# ── Frontend (Streamlit demo) ────────────────────────────────────────
streamlit>=1.35
plotly>=5.22
folium>=0.16
streamlit-folium>=0.20
matplotlib>=3.7

# ── Web Scraping ─────────────────────────────────────────────────────
beautifulsoup4>=4.12
lxml>=5.0
cloudscraper>=1.2.71
firecrawl-py>=1.5

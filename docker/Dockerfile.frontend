FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml .
RUN pip install --no-cache-dir streamlit plotly folium streamlit-folium httpx

COPY src/frontend ./src/frontend
COPY config ./config

EXPOSE 8501
CMD ["streamlit", "run", "src/frontend/app.py", "--server.address=0.0.0.0"]

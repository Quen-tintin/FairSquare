[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -e '.[dev]' || pip install -r requirements.txt"]

[start]
cmd = "streamlit run src/frontend/app.py --server.port $PORT --server.address 0.0.0.0"

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["python3.11 -m pip install -e '.[dev]' || python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "streamlit run src/frontend/app.py --server.port $PORT --server.address 0.0.0.0"
